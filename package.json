{
  "name": "anauseam-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspace anauseam-site-v2",
    "dev": "npm run dev --workspace anauseam-site-v2",
    "deploy": "npm run deploy --workspace anauseam-site-v2"
  }
}
